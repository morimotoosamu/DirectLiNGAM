% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_adjacency_diagrammer.r
\name{plot_adjacency_diagrammer}
\alias{plot_adjacency_diagrammer}
\title{隣接行列から DiagrammeR で因果グラフを描画}
\usage{
plot_adjacency_diagrammer(
  B,
  labels = NULL,
  threshold = 0.01,
  rankdir = "LR",
  graph_label = "Estimated Causal Structure",
  shape = "circle",
  fillcolor = "lightyellow",
  bordercolor = "black",
  fontsize_node = 14,
  fontsize_edge = 10,
  edge_color = "gray40",
  edge_label_color = "red",
  debug = FALSE
)
}
\arguments{
\item{B}{隣接行列}

\item{labels}{変数名ベクトル (NULL の場合は x0, x1, ... を自動生成)}

\item{threshold}{表示する最小係数の絶対値 (default: 0.01)}

\item{rankdir}{レイアウト方向 (default: "LR")
"LR" = 左→右, "RL" = 右→左, "TB" = 上→下, "BT" = 下→上}

\item{graph_label}{グラフのタイトル (default: "Estimated Causal Structure")}

\item{shape}{ノードの形状 (default: "circle")
"circle", "box", "ellipse", "diamond", "plaintext",
"square", "triangle", "hexagon", "octagon" など}

\item{fillcolor}{ノードの塗りつぶし色 (default: "lightyellow")}

\item{bordercolor}{枠線の色}

\item{fontsize_node}{ノードのフォントサイズ (default: 14)}

\item{fontsize_edge}{エッジラベルのフォントサイズ (default: 10)}

\item{edge_color}{エッジの色 (default: "gray40")}

\item{edge_label_color}{エッジラベルの色 (default: "red")}

\item{debug}{デバッグモードの有効化 (logical)}
}
\value{
grViz オブジェクト（DiagrammeR が利用可能な場合）
}
\description{
隣接行列から DiagrammeR で因果グラフを描画
}
\examples{
# 個々の実行例を書き直す
pk <- make_prior_knowledge(6, exogenous_variables = c(4))

# 書き直す
pk <- make_prior_knowledge(6,
  exogenous_variables = "x3",
  sink_variables = c("x1", "x4"),
  paths = list(c("x3", "x0"), c("x3", "x2")),
  no_paths = list(c("x5", "x2")),
  labels = c("x0", "x1", "x2", "x3", "x4", "x5")
)
}
